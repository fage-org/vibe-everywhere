# vibe-remote

This repository has been reset for a long-running Happy-aligned rebuild.

The rebuild baseline is now complete and remains plan-governed:

- the legacy `vibe-remote` implementation has been removed
- the target repository shape mirrors `/root/happy`
- `packages/vibe-app` has been imported from Happy and adapted for Vibe
- the Rust crates now implement the planned server, agent, CLI, wire, and sidecar surfaces
- remaining work is limited to parity audits and non-blocking follow-up documentation or packaging cleanup

Start from [PLAN.md](/root/vibe-remote/PLAN.md).
