# vibe-remote

This repository has been reset for a long-running Happy-aligned rebuild.

The current baseline is plan-first:

- the legacy `vibe-remote` implementation has been removed
- the target repository shape mirrors `/root/happy`
- `packages/vibe-app` will be imported from Happy and adapted
- the remaining subsystems will be rewritten in Rust

Start from [PLAN.md](/root/vibe-remote/PLAN.md).
