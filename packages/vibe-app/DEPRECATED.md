# Deprecated: `packages/vibe-app`

This package is no longer part of the active app pipeline.

Use `packages/vibe-app-tauri` for all active app work.

Use `packages/vibe-app` only when both of the following are true:

1. `/root/happy/packages/happy-app` does not answer the question
2. you need a Vibe-specific historical continuity reference

Do not use this package for:

- CI validation
- release builds
- OTA/update ownership
- new feature work
- new planning ownership
