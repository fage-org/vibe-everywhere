# v0.1.18

## Highlights

- unify agent task execution around ACP and add the local Codex ACP bridge path
- defer `Claude Code` ACP support and stop advertising it as available
- add app-side ACP integration coverage for both fake and local real `open_code`

## Validation

- `cargo fmt --all --check`
- `cargo check --locked -p vibe-relay -p vibe-agent -p vibe-app`
- `cargo test --locked --workspace --all-targets -- --nocapture`
- `cd apps/vibe-app && npm test`
- `cd apps/vibe-app && npm run build`
- `./scripts/dual-process-smoke.sh relay_polling`
- `./scripts/dual-process-smoke.sh overlay`
