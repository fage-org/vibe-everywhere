# Vibe Everywhere 页面信息架构与交互说明

## 1. 目的

本文档基于 `需求文档.md`，补充产品的信息架构、页面清单、导航关系和关键交互说明，用于后续产品设计、原型绘制和工程拆分。

本文档只覆盖首发版本范围，不展开企业能力、多用户能力和重度编辑器能力。

## 2. 信息架构原则

### 2.1 会话优先

产品的核心对象是“长期线程式会话”，不是主机管理页，也不是文件管理页。因此无论是手机端还是桌面端，都应优先让用户看到：

- 当前有哪些活动会话
- 哪些会话需要处理权限
- 哪些会话有新日志、新 diff、新异常

### 2.2 主机承载 workspace

workspace 不是独立顶层实体，而是远程主机下的工作目录。因此信息结构采用：

`主机 -> workspace -> 会话`

### 2.3 活动态与归档态分离

活动会话和归档会话必须分开展示，避免活动列表被历史记录淹没。

### 2.4 手机轻操作，桌面全功能

- 手机端聚焦：新建会话、看日志、看 diff、看文件树、批权限、发消息、做基础控制
- 桌面端聚焦：完整管理主机、workspace、活动会话、归档会话和设置

### 2.5 跨端结构尽量一致

虽然手机和桌面的布局不同，但对象模型、命名和关键动作应尽量一致，避免用户在不同设备上切换时产生额外学习成本。

## 3. 核心对象关系

```text
服务器
├─ 客户端设备
├─ 远程主机
│  ├─ Workspace
│  │  ├─ 活动会话
│  │  └─ 归档会话
│  └─ 守护进程状态
└─ 用户设置
   ├─ 通知配置
   ├─ 设备授权
   └─ 服务器连接配置
```

## 4. 手机端信息架构

## 4.1 全局导航

手机端采用底部四栏导航：

- `会话`
- `主机`
- `归档`
- `设置`

附加全局入口：

- 右上角通知中心入口
- 全局“新建会话”浮动按钮

### 4.1.1 默认首页

默认进入 `会话` 页，因为产品以会话为核心，且用户最常见的返回行为是继续已有会话。

## 4.2 手机端页面树

```text
手机端
├─ 启动与接入
│  ├─ 服务器地址配置
│  ├─ 当前设备授权完成页
│  └─ 主机配对页（扫码 / 输码）
├─ 会话
│  ├─ 活动会话列表
│  ├─ 新建会话
│  ├─ 会话详情
│  │  ├─ 概览
│  │  ├─ 日志
│  │  ├─ 变更 Diff
│  │  ├─ 文件树
│  │  └─ 会话操作菜单
│  ├─ 权限审批底板
│  └─ 发送消息 / 命令输入
├─ 主机
│  ├─ 主机列表
│  ├─ 主机详情
│  └─ Workspace 列表
├─ 归档
│  ├─ 归档会话列表
│  ├─ 归档会话详情
│  └─ 批量清理
└─ 设置
   ├─ 服务器连接设置
   ├─ 当前设备信息
   ├─ 通知设置
   └─ 关于与诊断
```

## 4.3 手机端主要页面说明

### M-01 服务器地址配置页

用途：

- 首次打开 App 时输入或编辑服务器地址
- 校验服务器是否可连接

核心元素：

- 服务器地址输入框
- 测试连接按钮
- 继续按钮

成功后进入设备授权完成页。

### M-02 当前设备授权完成页

用途：

- 告知当前手机设备已被服务器登记
- 引导用户去配对远程主机

核心元素：

- 当前设备名称
- 服务器连接状态
- “去配对主机”按钮
- “稍后再说”按钮

### M-03 主机配对页

用途：

- 扫描二维码配对远程主机
- 输入配对码完成远程主机绑定

核心元素：

- 扫码入口
- 配对码输入框
- 绑定说明
- 最近配对结果

### M-04 活动会话列表页

用途：

- 展示所有正在运行、待处理、暂停中的活动会话

列表分组建议：

- 需要我处理
- 运行中
- 已暂停

每个会话卡片展示：

- 会话标题
- 主机名
- workspace 名称
- agent 类型
- 最近活跃时间
- 当前状态
- 是否有待审批项

### M-05 新建会话页

用途：

- 创建一个新的长期会话

流程结构：

1. 选择主机
2. 选择或创建 workspace
3. 选择 agent
4. 输入首条任务消息
5. 创建会话

关键点：

- workspace 支持选择已有目录
- workspace 支持输入新目录并创建
- agent 首发默认 Claude Code，其他 ACP agent 可在后续扩展显示

### M-06 会话详情页

用途：

- 手机端最核心页面，用于查看和控制单个活动会话

页面结构建议：

- 顶部：会话标题、主机、workspace、agent、状态
- 状态提示区：连接状态、最近错误、待审批提醒
- 分段标签：`概览`、`日志`、`变更`、`文件`
- 底部输入区：发送消息 / 常规命令输入
- 更多菜单：暂停、终止、中断当前任务、重新运行、关闭并归档

### M-06-1 会话概览

展示内容：

- 最近一条 agent 回复
- 当前执行阶段
- 最新 diff 摘要
- 最新权限请求摘要
- 最近一次错误或异常

### M-06-2 会话日志

展示内容：

- 时间序日志流
- 支持自动滚动和手动暂停滚动
- 支持关键字检索

### M-06-3 会话变更 Diff

展示内容：

- 文件变更列表
- 按文件切换浏览 diff
- 基础检索

交互要求：

- 先看文件列表，再看单文件 diff
- 移动端不追求复杂代码审查能力，但要保证变更看得清楚

### M-06-4 文件树

展示内容：

- 当前 workspace 的完整文件树
- 文本文件内容只读浏览
- 非文本文件显示文件名、大小、类型、最后修改时间等元信息

### M-07 权限审批底板

用途：

- 在会话过程中处理高风险操作授权

展示字段：

- 风险类型
- 操作摘要
- 目标路径或命令
- 发起时间
- 来源会话

操作按钮：

- 单次允许
- 单次拒绝
- 本会话记住授权

### M-08 主机列表页

用途：

- 查看所有已配对主机
- 进入主机下的 workspace 列表

每个主机卡片展示：

- 主机名
- 在线状态
- 系统类型
- 守护进程状态
- 最近活跃时间

### M-09 主机详情 / Workspace 列表页

用途：

- 展示某台主机下的 workspace
- 直接从 workspace 发起新会话

列表组织：

- 收藏 workspace
- 其他 workspace

每个 workspace 项展示：

- 名称或目录名
- 路径
- 是否收藏
- 最近使用时间
- 快速“新建会话”入口

### M-10 归档会话列表页

用途：

- 查看所有归档会话
- 支持筛选和批量清理

筛选建议：

- 按主机
- 按 workspace
- 按 agent
- 按时间

### M-11 归档会话详情页

用途：

- 查看已关闭会话的历史上下文

支持查看：

- 消息历史
- 日志
- diff
- 文件树状态
- 元信息

限制：

- 不可继续运行
- 不显示发送消息入口

### M-12 设置页

用途：

- 管理服务器连接、设备信息、通知偏好和诊断信息

子模块建议：

- 服务器地址
- 当前设备信息
- 通知设置
- 关于与日志导出

### M-13 通知设置页

建议支持：

- 权限请求通知开关
- 任务完成通知开关
- 任务失败通知开关
- 会话异常通知开关
- 全局通知开关

## 5. 桌面端信息架构

## 5.1 全局导航

桌面端采用左侧主导航栏 + 中部列表区 + 右侧详情区的三栏或二栏结构。

主导航建议包括：

- `会话`
- `主机与工作区`
- `归档`
- `通知`
- `设置`

### 5.1.1 默认首页

默认进入 `会话` 页，并直接聚焦最近活动会话。

## 5.2 桌面端页面树

```text
桌面端
├─ 启动与接入
│  ├─ 服务器地址配置
│  ├─ 当前设备授权完成页
│  └─ 主机配对页
├─ 会话
│  ├─ 活动会话列表
│  ├─ 新建会话
│  ├─ 会话工作台
│  │  ├─ 概览
│  │  ├─ 日志
│  │  ├─ Diff
│  │  ├─ 文件树
│  │  └─ 会话操作区
│  └─ 权限审批面板
├─ 主机与工作区
│  ├─ 主机列表
│  ├─ 主机详情
│  ├─ Workspace 列表
│  └─ 收藏 Workspace
├─ 归档
│  ├─ 归档列表
│  ├─ 归档详情
│  └─ 批量清理
├─ 通知
│  ├─ 通知中心
│  └─ 通知偏好
└─ 设置
   ├─ 服务器连接
   ├─ 当前设备
   ├─ 诊断
   └─ 关于
```

## 5.3 桌面端主要页面说明

### D-01 活动会话列表页

用途：

- 作为桌面端默认主入口
- 快速进入任一活动会话

建议列表信息：

- 会话名称
- 所属主机
- workspace
- agent
- 状态
- 最近活跃时间
- 待审批标记

### D-02 会话工作台

用途：

- 桌面端核心工作页面

推荐布局：

- 左侧：活动会话列表
- 中间：当前会话主内容区
- 右侧：上下文信息 / 权限请求 / 会话属性

主内容区建议采用标签页：

- 概览
- 日志
- Diff
- 文件树

底部保留：

- 消息输入框
- 常规命令输入

顶部工具栏：

- 暂停
- 终止
- 中断当前任务
- 重新运行
- 关闭并归档

### D-03 主机与工作区管理页

用途：

- 集中管理主机和其下 workspace

推荐布局：

- 左列主机列表
- 中列该主机的 workspace 列表
- 右列 workspace 详情或快捷动作

关键动作：

- 配对新主机
- 新建 workspace
- 收藏 workspace
- 从某个 workspace 直接新建会话

### D-04 归档中心

用途：

- 集中查看归档会话并做批量清理

能力建议：

- 多条件筛选
- 列表 + 详情联动
- 批量删除归档

### D-05 通知中心

用途：

- 查看桌面端收到的重要通知
- 快速跳转到对应会话

建议通知类型：

- 权限请求
- 任务完成
- 任务失败
- 会话异常

### D-06 设置页

建议模块：

- 服务器地址配置
- 当前设备信息
- 通知偏好
- 诊断信息

## 6. 跨端一致性设计

以下对象在手机端和桌面端必须保持一致：

- 主机名称
- workspace 名称和路径
- 会话名称
- 会话状态定义
- 权限按钮命名
- 会话操作命名
- 归档状态定义

建议统一的状态标签：

- 运行中
- 待处理
- 已暂停
- 异常
- 已关闭
- 已归档

## 7. 关键交互流程

## 7.1 首次接入流程

1. 用户打开客户端
2. 输入服务器地址并测试连接
3. 当前设备获得服务器授权
4. 用户进入主机配对页
5. 远程主机运行守护进程并生成配对码或二维码
6. 用户扫码或输码完成主机绑定
7. 客户端展示主机列表

## 7.2 从手机端新建会话

1. 用户进入 `会话` 或 `主机` 页
2. 点击“新建会话”
3. 选择主机
4. 选择已有 workspace 或创建新目录
5. 选择 agent
6. 输入首条任务消息
7. 创建成功后进入会话详情页

## 7.3 处理权限请求

1. agent 触发高风险操作
2. 服务端将权限请求推送到客户端
3. 会话详情页显示待审批提醒，同时可触发系统通知
4. 用户打开权限审批底板
5. 选择单次允许、单次拒绝或本会话记住授权
6. 结果同步回守护进程并继续流程

## 7.4 跨端接力

1. 用户在桌面端创建并运行会话
2. 用户离开桌面后在手机端打开同一会话
3. 手机端继续接收日志、diff、权限请求和状态变更
4. 用户在手机端追加消息或做权限审批
5. 回到桌面端后继续进入同一会话，状态保持一致

## 7.5 关闭并归档

1. 用户在手机端或桌面端点击“关闭并归档”
2. 系统要求确认
3. 会话关闭并从活动列表移除
4. 会话进入归档列表
5. 用户后续只能查看历史，不能继续执行

## 8. 页面优先级

## 8.1 P0 页面

- 服务器地址配置
- 主机配对
- 活动会话列表
- 新建会话
- 会话详情
- 权限审批底板
- 主机列表
- 主机详情 / workspace 列表
- 归档列表
- 设置页

## 8.2 P1 页面

- 通知中心
- 归档批量清理
- 诊断页

## 9. 页面与模块映射建议

便于后续设计和研发拆分，可按以下模块组织：

- 接入模块：服务器配置、设备授权、主机配对
- 会话模块：活动列表、新建会话、会话详情、会话控制、权限审批
- 主机模块：主机列表、主机详情、workspace 管理
- 归档模块：归档列表、归档详情、批量清理
- 设置模块：通知、服务器、设备、诊断

## 10. 关键设计注意事项

- 手机端会话详情页必须足够轻，避免一次展示过多层级
- Diff 浏览要优先保证可读性，而不是追求复杂代码审查交互
- 文件树浏览必须明确区分文本文件和非文本文件
- “关闭并归档”要与“终止当前任务”明显区分，避免误操作
- 待审批会话需要在列表中有高优先级展示
- 主机离线、守护进程异常、会话断连等状态必须有统一提示样式

## 11. 当前默认设计假设

为了推进原型设计，当前先采用以下默认假设：

- 手机端默认首页是“会话”
- 桌面端默认首页也是“会话”
- 新建会话入口在“会话页”和“主机详情页”都提供
- 归档会话与活动会话彻底分离
- 通知中心在桌面端作为独立入口，手机端以系统通知 + 设置页配置为主

如果后续需要继续细化，下一步建议补：

- 页面级字段清单
- 页面状态机
- 关键页面低保真线框
- 设计系统中的颜色和状态规范
